<h1>Battle#test</h1>
<p>Find me in app/views/battle/test.html.erb</p>

<h1>enumの練習だよ</h1>
<h1>Battle#test</h1>
<!%= form_for(@status) do |f| %>
  <%= select :status, :name, Battle.statuses.keys.to_a %>
  <!%= submit_tag "登録" %>
<!% end %>





<!%= select :status, :name, Battle.statuses.values.to_a %>
<!%= select :status, :nmae, Battle.statuses.values.to_a %>
